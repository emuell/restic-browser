{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build debug",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "command": "wails",
      "args": [
        "build",
        "-debug"
      ]
    },
    {
      "label": "build",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "command": "wails",
      "args": [
        "build"
      ]
    }
  ]
}